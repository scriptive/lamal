home:function(resolve, reject){
  // var configuration = app.config, local = app.localStorage, ol=document.createElement('ol');
  // app.Toggle.main(true).appendChild(ol).setAttribute('class','main-language');
  // var li =document.createElement('li');
  // li.innerHTML='Hello';
  // ol.appendChild(li);
  // resolve();
  reject('device Ready');
},